phases:
  artifacts:
    files:
    - file.xml
    base-directory: base
  build:
    commands:
    - echo "Executando os testes no ambiente de DEV"
    - cd tests/integrations
    - python -m pytest -M "nova versao python" -E dev --junitxml=relatorio.xml ||
      true
  install:
    commands: commands
    runtime-versions:
      python: 3.8
version: 0.2
