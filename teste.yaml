version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.8
    commands: commands
  build:
    commands: "        \n      - echo \"Executando os testes no ambiente de DEV\"\n\
      \      - cd tests/integrations\n      - python -m pytest -M \"\" -E hom --junitxml=relatorio.xml\
      \ || true\n    "
  artifacts:
    base-directory: base
    files:
    - file.xml
