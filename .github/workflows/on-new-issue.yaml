on:
  issues:
    types:
      - opened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      
      - name: Convert Form Issu to JSON
        run : |
          echo ${{ steps.payload.outputs.json_var }}
